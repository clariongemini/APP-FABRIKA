# AI Studio minimal export (fixture)

Tek modül, hard-coded string — tipik Stitch/AI Studio ham çıktı simülasyonu.

`test/bootstrap-aistudio-lab.sh` bu fixture'ı `test/aistudio-lab-run/` altına kopyalar ve `bootstrap-external-project.sh` çalıştırır.

**Commit edilmez:** `test/aistudio-lab-run/` (`.gitignore`)
