# Proven Patterns

Patterns promoted from real shipped apps. **Empty until first promotion.**

```bash
python3 scripts/factory/promote-pattern.py --name offline_first --slug my-app --evidence "..."
```

Intelligence engine prefers proven over experimental when ranking recommendations.
